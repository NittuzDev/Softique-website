{
  "name": "softique-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Softique Beauty Nail — static site + Cloudflare Worker booking API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv namespace create BOOKING_KV"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
